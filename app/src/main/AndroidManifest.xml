<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.challenge">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/iconcalculatorimage"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/iconcalculatorimage"
        android:supportsRtl="true"
        android:theme="@style/Theme.Challenge">
        <activity
            android:windowSoftInputMode="adjustPan|adjustResize"
            android:name="com.example.challenge.ui.CalculatorActivity"
            android:label="@string/app_name"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.Challenge.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>